# slam_toolbox online-async mapping config for blaze_racer.
#
# Frames match what the subsystem stack actually publishes:
#   odom  -> base_link : the VESC /odom, re-broadcast as TF by
#                        blaze_slam/odom_tf_publisher (or by vesc_to_odom if you
#                        set publish_tf: true).
#   base_link -> laser : static TF from bringup_launch.py (0.27, 0, 0.11).
#   map  -> odom       : published by slam_toolbox (this node).
#   scan topic         : /scan from urg_node (frame_id 'laser').
#
# NOTE: base_frame MUST be base_link, not laser. An earlier config used
# base_frame: laser, which makes slam_toolbox track the laser instead of the
# robot body and corrupts the map.

slam_toolbox:
  ros__parameters:

    # Pose-graph solver.
    solver_plugin: solver_plugins::CeresSolver
    ceres_linear_solver: SPARSE_NORMAL_CHOLESKY
    ceres_preconditioner: SCHUR_JACOBI
    ceres_trust_strategy: LEVENBERG_MARQUARDT
    ceres_dogleg_type: TRADITIONAL_DOGLEG
    ceres_loss_function: None

    # Frames and input topic (REAL car).
    odom_frame: odom
    map_frame: map
    base_frame: base_link
    scan_topic: /scan
    mode: mapping              # mapping | localization

    # Runtime behaviour.
    debug_logging: false
    throttle_scans: 1
    transform_publish_period: 0.02   # 50 Hz map->odom TF
    map_update_interval: 2.0         # publish /map every 2 s
    resolution: 0.05                 # metres per pixel (5 cm)
    max_laser_range: 15.0            # clamp Hokuyo returns used for mapping
    minimum_time_interval: 0.2
    # Be tolerant of brief odom TF lag under CPU load on the Jetson.
    transform_timeout: 0.5
    tf_buffer_duration: 30.0
    stack_size_to_use: 40000000
    enable_interactive_mode: true

    # Process a new keyframe only after this much motion (keeps graph compact).
    use_scan_matching: true
    use_scan_barycenter: true
    minimum_travel_distance: 0.2
    minimum_travel_heading: 0.2
    scan_buffer_size: 10
    scan_buffer_maximum_scan_distance: 10.0
    link_match_minimum_response_fine: 0.1
    link_scan_maximum_distance: 1.5

    # Loop closure (straightens the map when the lap closes).
    do_loop_closing: true
    loop_search_maximum_distance: 3.0
    loop_match_minimum_chain_size: 10
    loop_match_maximum_variance_coarse: 3.0
    loop_match_minimum_response_coarse: 0.35
    loop_match_minimum_response_fine: 0.45

    # Correlation (scan matching) search space.
    correlation_search_space_dimension: 0.5
    correlation_search_space_resolution: 0.01
    correlation_search_space_smear_deviation: 0.1

    # Correlation search space for loop closure.
    loop_search_space_dimension: 8.0
    loop_search_space_resolution: 0.05
    loop_search_space_smear_deviation: 0.03

    # Scan matcher penalties / resolution.
    distance_variance_penalty: 0.5
    angle_variance_penalty: 1.0
    fine_search_angle_offset: 0.00349
    coarse_search_angle_offset: 0.349
    coarse_angle_resolution: 0.0349
    minimum_angle_penalty: 0.9
    minimum_distance_penalty: 0.5
    use_response_expansion: true

    # Optimise the graph periodically.
    optimize_every_n_nodes: 20
